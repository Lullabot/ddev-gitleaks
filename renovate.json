{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "helpers:pinGitHubActionDigests"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/web-build/Dockerfile\\.gitleaks$/"
      ],
      "matchStrings": ["ARG GITLEAKS_VERSION=(?<currentValue>\\S+)"],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "gitleaks/gitleaks",
      "extractVersionTemplate": "^v(?<version>.+)$"
    }
  ],
  "automerge": true,
  "minimumReleaseAge": "3 days",
  "packageRules": [
    {
      "matchManagers": ["github-actions"],
      "automerge": false
    },
    {
      "description": "Review gitleaks bumps manually: a bad version breaks the web image build (curl of a missing release asset), which is the one path the scanner's never-block guarantee does not cover.",
      "matchDepNames": ["gitleaks/gitleaks"],
      "automerge": false
    }
  ]
}
